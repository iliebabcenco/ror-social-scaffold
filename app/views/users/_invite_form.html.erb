<div class='invite-friend-form'>
  <%= form_for(user.received_requests.new, url: user_friendships_path(current_user)) do |form| %>
    <%= form.hidden_field :invitee_id, value: user.id %>
    <%= form.submit 'Send Invite', class: 'link-btn' %>
  <% end %>
</div>
